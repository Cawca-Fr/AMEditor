<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.AMEditor" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status Bar configuration for light background with dark icons -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>

        <item name="toolbarStyle">@style/Widget.AMEditor.Toolbar</item>
    </style>

    <style name="Widget.AMEditor.Toolbar" parent="Widget.Material3.Toolbar">
        <item name="titleTextAppearance">@style/TextAppearance.AMEditor.Toolbar.Title</item>
    </style>

    <style name="TextAppearance.AMEditor.Toolbar.Title" parent="TextAppearance.Material3.TitleLarge">
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- Thème appliqué à la Toolbar : texte blanc pour le titre et les icônes -->
    <style name="ThemeOverlay.AMEditor.Toolbar" parent="ThemeOverlay.Material3.Dark.ActionBar">
        <item name="colorControlNormal">@color/white</item>
        <item name="android:textColorPrimary">@color/white</item>
    </style>

    <!-- Thème appliqué au menu contextuel (Overflow) : fond blanc et texte noir -->
    <style name="AMEditor.OverflowMenu" parent="Widget.Material3.PopupMenu.Overflow">
        <item name="android:textColor">@color/black</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:background">@color/white</item>
    </style>

    <style name="ThemeOverlay.AMEditor.Popup" parent="ThemeOverlay.Material3.Light">
        <item name="android:textColor">@color/black</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:background">@color/white</item>
        <!-- On lie le style spécifique de l'overflow pour éviter le double affichage/animation -->
        <item name="actionOverflowMenuStyle">@style/AMEditor.OverflowMenu</item>
    </style>
</resources>
